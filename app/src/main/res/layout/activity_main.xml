<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".view.MainActivity">

    <FrameLayout
        android:id="@+id/layoutMain"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/layoutBar"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/blue"
        app:layout_constraintBottom_toTopOf="@+id/layoutBar" />

    <LinearLayout
        android:id="@+id/layoutBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/txtButtonHome"
            style="@style/txtButtonBar"
            android:text="@string/home"
            app:drawableTopCompat="@mipmap/ic_home" />

        <TextView
            android:id="@+id/txtButtonSearch"
            style="@style/txtButtonBar"
            android:text="@string/search"
            app:drawableTopCompat="@mipmap/ic_search" />

        <TextView
            android:id="@+id/txtButtonLibrary"
            style="@style/txtButtonBar"
            android:text="@string/library"
            app:drawableTopCompat="@mipmap/ic_library" />

        <TextView
            android:id="@+id/txtButtonSetting"
            style="@style/txtButtonBar"
            android:text="@string/setting"
            app:drawableTopCompat="@mipmap/ic_settings" />

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>